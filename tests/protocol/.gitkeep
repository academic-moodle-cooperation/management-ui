# The imported protocol YAML and its raw export are gitignored — org data.
# Only this placeholder and the README are tracked, so the directory exists in a
# fresh clone. See README.md for the import workflow.
