# Sanitized HAR recordings go here. The *.har files are gitignored on purpose —
# they carry an org's real config and data shapes. Only this placeholder is
# tracked, so the directory exists in a fresh clone.
#
# See ../../../docs/contribute/manual-test-recording.md
