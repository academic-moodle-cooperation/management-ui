---
---

Docs: replace the two ~1.4k-line `docs/workflows/ADDING_APPS.md` +
`ADDING_PACKAGES.md` (dangling links to deleted docs, stale paths like
`apps/management-ui-core`, excluded from the site) with a single concise,
principle-based `docs/operations/extending-the-workspace.md`. Covers
adding a top-level package or app — pointing at real examples and current
docs rather than re-deriving brittle 18-step copy-paste — added to the
Operations sidebar and dropped from `srcExclude`. Closes open-followups
§8.1. Docs-only.
