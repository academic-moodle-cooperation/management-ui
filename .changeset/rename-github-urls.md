---
---

Update GitHub repository URLs after the `management-tool` → `management-ui` repo rename (docs, `package.json` `homepage`/`repository`/`bugs` metadata, issue templates, scaffolding templates, and the `release.yml` `github.repository` guard). Metadata only — no code or API change — so no release is required; the corrected URLs ship with the next release. GitHub redirects keep the old URLs working in the meantime.
