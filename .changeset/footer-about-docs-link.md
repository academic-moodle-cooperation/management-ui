---
"@oc-mui/plugin-core": patch
---

The footer's "About Management UI" link points at the published documentation site instead of the app's home page. It pointed home only while the docs URL 404'd on the private repo.
