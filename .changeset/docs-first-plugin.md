---
---

Docs: add a hands-on "Your first plugin" tutorial
(`docs/plugins/first-plugin.md`) and link it first in the Plugins
sidebar. It walks through `pnpm create-plugin <name> --template app` →
build → enable → see the sidebar entry + page render, then summarizes the
four delivery paths (dev server, in-tree, JAR, CDN/marketplace). Captures
the visible-plugin flow added in the `--template app` work; complements
the `creating-a-plugin.md` reference rather than duplicating it.

Docs-only.
