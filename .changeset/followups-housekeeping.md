---
---

Docs: open-followups maintenance. Mark §4.1 (api-extractor TSDoc
warnings) ✅ Done — verified clean across all six API-snapshot packages.
Add §5.5 — the auth-error screen (`AuthCheckError`) dumps the raw GraphQL
error blob to the user; trim to a friendly summary + a collapsible
details expander. Docs-only.
