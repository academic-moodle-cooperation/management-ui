---
"@oc-mui/plugin-admin-marketplace": patch
---

Recolor the marketplace **Example** theme's primary to Harvard Crimson
(`#A51C30`, `oklch(0.45 0.165 20)` light / `oklch(0.60 0.19 22)` dark). It
stays a minimal single-token reference theme — only `--primary` (+ matching
`--primary-foreground`/`--ring`) changes — now demonstrating a recognizable
brand recolor instead of a muted teal. Registry description updated to match.
