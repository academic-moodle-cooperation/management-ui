---
---

Docs site: add robots.txt + a `noindex` meta tag so the VitePress site
stays out of search engines while the project is pre-1.0. Belt-and-
suspenders with the commented-out push trigger in the docs deploy
workflow — all three guards flip together when going public (full
checklist consolidated in `docs/operations/open-followups.md` §8.3).

No package code touched. Empty changeset records the doc-tooling
nature of the change.
