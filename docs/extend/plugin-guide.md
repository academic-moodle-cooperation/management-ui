# Building a plugin

For plugin developers past the scaffold. Afterwards you'll know the manifest, the extension points, the config slice, and the import boundaries your plugin lives inside.

Coming in the next package.
