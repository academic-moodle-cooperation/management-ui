# Add a GraphQL field

For developers who need data the API does not expose yet. Afterwards you'll have a new field available end to end, from the Java type to the generated client.

Coming in the next package.
