## API Report File for "@opencast-mui/store"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { atom } from 'jotai';
import { atomWithStorage } from 'jotai/utils';
import { create } from 'zustand';
import { createStore } from 'jotai';
import { PersistOptions } from 'zustand/middleware';
import { PrimitiveAtom } from 'jotai';
import { Provider } from 'jotai';
import { StoreApi } from 'zustand';
import { useAtom } from 'jotai';
import { useAtomValue } from 'jotai';
import { UseBoundStore } from 'zustand';
import { useSetAtom } from 'jotai';

export { atom }

export { atomWithStorage }

export { create }

export { createStore }

export { PrimitiveAtom }

export { Provider }

// @public
export type Store = {
    zustandupload: UploadListType;
    setUpload: (upload: UploadListType) => void;
    submitUpload: () => void;
    nextUpload: (uploadListFile: UploadFileBlob) => void;
    deleteUpload: (id: number) => void;
    resetUpload: () => void;
    updateFile: (updateFileInfo: UploadFileBlob) => void;
    fileUploaded: (uploadFile: UploadFileBlob, pending: UploadFileBlob[]) => void;
    filesUploaded: () => void;
    setUploadError: (uploadError: Error) => void;
};

// @public
export type UploadFileBlob = {
    file: Blob;
    id: number;
    src: string;
    name: string;
    uploadName: string;
    type: string;
    displayType?: string;
    fileInfo?: unknown[];
    analyzed?: boolean;
    size: number;
    progress: number;
    status: string;
    request?: XMLHttpRequest;
};

// @public
export type UploadListType = {
    files: UploadFileBlob[];
    seriesId: string;
    pending: UploadFileBlob[];
    next: UploadFileBlob | null;
    uploading: boolean;
    uploaded: UploadFileBlob[];
    status: string;
    uploadError: Error | null;
};

export { useAtom }

export { useAtomValue }

export { useSetAtom }

// @public
export const useStore: UseBoundStore<Omit<StoreApi<Store>, "persist"> & {
persist: {
setOptions: (options: Partial<PersistOptions<Store, Store>>) => void;
clearStorage: () => void;
rehydrate: () => Promise<void> | void;
hasHydrated: () => boolean;
onHydrate: (fn: (state: Store) => void) => () => void;
onFinishHydration: (fn: (state: Store) => void) => () => void;
getOptions: () => Partial<PersistOptions<Store, Store>>;
};
}>;

// (No @packageDocumentation comment for this package)

```
