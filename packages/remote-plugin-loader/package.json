{
  "name": "@opencast-mui/remote-plugin-loader",
  "version": "1.0.0",
  "license": "ECL-2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf dist && rm -rf node_modules && rm -f *.tsbuildinfo",
    "lint": "eslint . --ext .ts --report-unused-disable-directives --max-warnings 0",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@opencast-mui/plugin-system": "workspace:*",
    "@opencast-mui/utils": "workspace:*"
  },
  "devDependencies": {
    "@opencast-mui/eslint-config": "workspace:*",
    "@opencast-mui/typescript-config": "workspace:*",
    "eslint": "^9.9.0",
    "typescript": "~5.5.4",
    "vitest": "^4.0.17"
  }
}
