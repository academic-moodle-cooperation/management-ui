{
  "name": "@workspace/plugin-core-series",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'core-series plugin does not run standalone'",
    "build": "echo 'No build step required for library plugin'",
    "clean": "rm -rf .turbo && rm -rf dist && rm -rf node_modules && rm -f *.tsbuildinfo && rm -f tsconfig*.tsbuildinfo",
    "lint": "eslint . --ext .ts,.tsx --report-unused-disable-directives --max-warnings 0",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace/i18n": "workspace:*",
    "@workspace/plugin-system": "workspace:*",
    "@workspace/providers": "workspace:*",
    "@workspace/query": "workspace:*",
    "@workspace/router": "workspace:*",
    "@workspace/store": "workspace:*",
    "@workspace/ui": "workspace:*",
    "@workspace/ui-config": "workspace:*",
    "@workspace/utils": "workspace:*",
    "lucide-react": "^0.417.0",
    "react": "^19.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/react": "^18.0.0 || ^19.0.0",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "eslint": "^9.20.0",
    "typescript": "^5.3.3",
    "vitest": "^4.0.17"
  }
}
