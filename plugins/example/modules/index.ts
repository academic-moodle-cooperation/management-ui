/**
 * Example plugin module barrel.
 *
 * Add new module exports here as you grow the example. Keep each module tiny
 * and focused on a single extension point so it can be read in under a
 * minute by a new contributor or an AI agent.
 */
export { exampleHeaderLogoPlugin } from "./header-logo-example";
