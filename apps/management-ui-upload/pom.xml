<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>management-frontend</artifactId>
    <groupId>org.amc.management</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>management-ui-upload</artifactId>
  <packaging>bundle</packaging>

  <name>Opencast Management Tool :: UI :: Plugin upload</name>
  <properties>
    <opencast.basedir>${project.basedir}/../..</opencast.basedir>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>install node and pnpm</id>
            <phase>initialize</phase>
            <goals>
              <goal>install-node-and-pnpm</goal>
            </goals>
          </execution>
          <execution>
            <id>pnpm install</id>
            <phase>initialize</phase>
            <goals>
              <goal>pnpm</goal>
            </goals>
            <configuration>
              <arguments>install --engine-strict</arguments>
              <workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <nodeVersion>v20.16.0</nodeVersion>
          <pnpmVersion>v10.4.1</pnpmVersion>
          <workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
          <installDirectory>${project.build.directory}</installDirectory>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>pnpm run build</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>${project.build.directory}/node/node</executable>
              <arguments>
                <argument>${project.build.directory}/node/node_modules/pnpm/bin/pnpm.cjs</argument>
                <argument>run</argument>
                <argument>build</argument>
                <argument>--filter=management-ui-upload</argument>
              </arguments>
              <workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
              <environmentVariables>
                <PATH>${project.build.directory}/node:${env.PATH}</PATH>
              </environmentVariables>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-resources</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${basedir}/target/classes/ui/static/plugins/upload</outputDirectory>
              <resources>
                <resource>
                  <directory>${basedir}/dist</directory>
                  <filtering>false</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <configuration>
          <instructions>
            <Private-Package>ui.*</Private-Package>
            <Management-Plugin>upload</Management-Plugin>
            <Fragment-Host>management-ui-core</Fragment-Host>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>org.amc.management</groupId>
      <artifactId>management-ui-core</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

</project> 